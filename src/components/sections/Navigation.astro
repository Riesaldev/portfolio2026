---
import DarkModeToggle from "@/components/DarkModeToggle.tsx";
import LanguageSwitcher from "@/components/LanguageSwitcher.tsx";
---

<nav
  id="navbar"
  class="sticky top-0 w-full z-40 transition-all duration-300"
  style="-webkit-backdrop-filter:blur(8px);backdrop-filter:blur(8px);"
>
  <div class="w-full px-6 md:px-12 py-4 flex items-center">
    <!-- Logo -->
    <div class="flex-1 flex items-center">
      <a
        href="#home"
        class="text-4xl font-display font-extrabold text-charcoal dark:text-white transition-colors"
      >
        <span class="gradient-text">Maria Isabel</span>
      </a>
    </div>

    <!-- Desktop Menu -->
    <ul class="hidden md:flex flex-1 justify-center items-center gap-8">
      <li>
        <a
          href="#home"
          class="nav-link text-charcoal-light dark:text-gray-300 font-medium transition-all duration-300 hover:text-coral dark:hover:text-coral-light relative lang-text"
          data-en="Home"
          data-sv="Hem">Home</a
        >
      </li>
      <li>
        <a
          href="#about"
          class="nav-link text-charcoal-light dark:text-gray-300 font-medium transition-all duration-300 hover:text-coral dark:hover:text-coral-light relative lang-text"
          data-en="About"
          data-sv="Om Mig">About</a
        >
      </li>
      <li>
        <a
          href="#journey"
          class="nav-link text-charcoal-light dark:text-gray-300 font-medium transition-all duration-300 hover:text-coral dark:hover:text-coral-light relative lang-text"
          data-en="Journey"
          data-sv="Resa">Journey</a
        >
      </li>
      <li>
        <a
          href="#skills"
          class="nav-link text-charcoal-light dark:text-gray-300 font-medium transition-all duration-300 hover:text-coral dark:hover:text-coral-light relative lang-text"
          data-en="Skills"
          data-sv="Kompetens">Skills</a
        >
      </li>
      <li>
        <a
          href="#contact"
          class="btn-primary lang-text"
          data-en="Contact"
          data-sv="Kontakt">Contact</a
        >
      </li>
    </ul>

    <!-- Toggles + Mobile Button -->
    <div class="flex-1 flex justify-end items-center gap-4">
      <div class="Toggles hidden md:flex items-center gap-4">
        <LanguageSwitcher client:only="react" />
        <DarkModeToggle client:only="react" />
      </div>

      <!-- Mobile Menu Button -->
      <button
        id="mobile-menu-btn"
        class="md:hidden w-10 h-10 flex flex-col items-center justify-center gap-1.5 group"
        aria-label="Toggle menu"
      >
        <span
          class="w-6 h-0.5 bg-charcoal dark:bg-white transition-all duration-300 group-[.active]:rotate-45 group-[.active]:translate-y-2"
        ></span>
        <span
          class="w-6 h-0.5 bg-charcoal dark:bg-white transition-all duration-300 group-[.active]:opacity-0"
        ></span>
        <span
          class="w-6 h-0.5 bg-charcoal dark:bg-white transition-all duration-300 group-[.active]:-rotate-45 group-[.active]:-translate-y-2"
        ></span>
      </button>
    </div>
  </div>

  <!-- Mobile Menu -->
  <div
    id="mobile-menu"
    class="md:hidden max-h-0 overflow-hidden transition-all duration-300 bg-white/95 dark:bg-gray-900/95"
    style="backdrop-filter:blur(16px);-webkit-backdrop-filter:blur(16px);box-shadow:0 8px 32px rgba(0,0,0,0.12);"
  >
    <ul class="flex flex-col items-center gap-6 py-8">
      <li>
        <a
          href="#home"
          class="nav-link mobile-nav-link text-2xl text-charcoal-light dark:text-gray-300 font-medium transition-all duration-300 hover:text-coral dark:hover:text-coral-light relative lang-text"
          data-en="Home"
          data-sv="Hem">Home</a
        >
      </li>
      <li>
        <a
          href="#about"
          class="nav-link mobile-nav-link text-2xl text-charcoal-light dark:text-gray-300 font-medium transition-all duration-300 hover:text-coral dark:hover:text-coral-light relative lang-text"
          data-en="About"
          data-sv="Om Mig">About</a
        >
      </li>
      <li>
        <a
          href="#journey"
          class="nav-link mobile-nav-link text-2xl text-charcoal-light dark:text-gray-300 font-medium transition-all duration-300 hover:text-coral dark:hover:text-coral-light relative lang-text"
          data-en="Journey"
          data-sv="Resa">Journey</a
        >
      </li>
      <li>
        <a
          href="#skills"
          class="nav-link mobile-nav-link text-2xl text-charcoal-light dark:text-gray-300 font-medium transition-all duration-300 hover:text-coral dark:hover:text-coral-light relative lang-text"
          data-en="Skills"
          data-sv="Kompetens">Skills</a
        >
      </li>
      <li>
        <a
          href="#contact"
          class="btn-primary lang-text"
          data-en="Contact"
          data-sv="Kontakt">Contact</a
        >
      </li>
      <li class="flex items-center gap-3 pt-2">
        <LanguageSwitcher client:only="react" />
        <DarkModeToggle client:only="react" />
      </li>
    </ul>
  </div>
</nav>

<style>
  .nav-link::after {
    content: "";
    position: absolute;
    bottom: 0;
    left: 0;
    width: 0;
    height: 2px;
    background-color: var(--color-coral);
    transition: width 0.3s ease;
  }

  .nav-link:hover::after,
  .nav-link.active::after {
    width: 100%;
  }

  #navbar.scrolled {
    background-color: rgba(255, 255, 255, 0.9);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.08);
  }

  :global(.dark) #navbar.scrolled {
    background-color: rgba(17, 24, 39, 0.9);
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.3);
  }
</style>

<script>
  // Smooth scroll
  document
    .querySelectorAll<HTMLAnchorElement>('a[href^="#"]')
    .forEach((anchor) => {
      anchor.addEventListener("click", (e) => {
        e.preventDefault();
        const href = anchor.getAttribute("href");
        const target = href ? document.querySelector(href) : null;
        if (target) {
          target.scrollIntoView({ behavior: "smooth" });
          // Close mobile menu if open
          const mobileMenu = document.getElementById("mobile-menu");
          const mobileMenuBtn = document.getElementById("mobile-menu-btn");
          if (mobileMenu && mobileMenuBtn) {
            mobileMenu.style.maxHeight = "0";
            mobileMenuBtn.classList.remove("active");
          }
        }
      });
    });

  // Mobile menu toggle
  const mobileMenuBtn = document.getElementById("mobile-menu-btn");
  const mobileMenu = document.getElementById("mobile-menu");

  mobileMenuBtn?.addEventListener("click", () => {
    mobileMenuBtn.classList.toggle("active");
    if (mobileMenu) {
      if (mobileMenu.style.maxHeight === "0px" || !mobileMenu.style.maxHeight) {
        mobileMenu.style.maxHeight = mobileMenu.scrollHeight + "px";
      } else {
        mobileMenu.style.maxHeight = "0";
      }
    }
  });

  // Navbar scroll effect
  const navbar = document.getElementById("navbar");
  let lastScroll = 0;

  window.addEventListener("scroll", () => {
    const currentScroll = window.pageYOffset;

    if (currentScroll > 50) {
      navbar?.classList.add("scrolled");
    } else {
      navbar?.classList.remove("scrolled");
    }

    // Active section highlighting
    const sections = document.querySelectorAll("section[id]");
    sections.forEach((section) => {
      const sectionTop = (section as HTMLElement).offsetTop - 100;
      const sectionHeight = (section as HTMLElement).offsetHeight;
      const sectionId = section.getAttribute("id");

      if (
        currentScroll >= sectionTop &&
        currentScroll < sectionTop + sectionHeight
      ) {
        document.querySelectorAll(".nav-link").forEach((link) => {
          link.classList.remove("active");
          if (link.getAttribute("href") === `#${sectionId}`) {
            link.classList.add("active");
          }
        });
      }
    });

    lastScroll = currentScroll;
  });
</script>
